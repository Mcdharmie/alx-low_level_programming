#include <stdio.h>
/**
 *main - printing the alphabet
 *Return: Always zero
 */

int main(void)

{
char letter;
for (letter = 'a'; letter <= 'z'; letter++)
{
  if ((letter = 'e' && letter != 'q'))
putchar(letter);
}
putchar('\n');
return (0);
}
